# Research Atlas: the master map of BSD approaches

> The strategic reference for the whole repo. Every known approach to BSD, what it proves, where it stops, and which wrong-approach detector catches its failure mode. Start here for the landscape; go to [`docs/solutions/`](../solutions/) for per-approach detail and [`docs/03_research/`](../03_research/) for the proof program.

## The statement, pinned

**Weak BSD.** For an elliptic curve $E/\mathbb{Q}$,
$$\operatorname{ord}_{s=1} L(E, s) = \operatorname{rank} E(\mathbb{Q}).$$
The left side is the analytic rank (order of vanishing of the Hasse-Weil $L$-function at the central point $s = 1$). The right side is the Mordell-Weil rank.

**Strong BSD.** With $r = \operatorname{rank} E(\mathbb{Q})$,
$$\lim_{s \to 1} \frac{L(E, s)}{(s-1)^r} = \frac{\Omega_E \cdot \operatorname{Reg}_E \cdot \prod_p c_p \cdot \#\operatorname{Sha}(E)}{(\#E(\mathbb{Q})_{\mathrm{tors}})^2},$$
where $\Omega_E$ is the real period, $\operatorname{Reg}_E$ the regulator of the Neron-Tate height pairing, $c_p$ the Tamagawa numbers, $\#E(\mathbb{Q})_{\mathrm{tors}}$ the order of the torsion subgroup, and $\operatorname{Sha}(E)$ the Tate-Shafarevich group. The formula presupposes $\operatorname{Sha}(E)$ is finite.

## What is proven, exactly

| Regime | Status | By whom |
|---|---|---|
| Analytic rank 0 | Rank equality + finite Sha + strong-form $p$-part (many $p$) PROVEN | Coates-Wiles (rank 0 part, CM), Kolyvagin, Rubin, Kato, Skinner-Urban |
| Analytic rank 1 | Rank equality + finite Sha PROVEN | Gross-Zagier 1986 + Kolyvagin 1990 |
| Analytic rank $\geq 2$ | OPEN. Neither rank equality nor finite Sha is known | -- |
| Modularity of $E/\mathbb{Q}$ | THEOREM (gives continuation + functional equation + root number) | Wiles; Taylor-Wiles; Breuil-Conrad-Diamond-Taylor 2001 |
| Parity conjecture | THEOREM (rank mod 2 matches analytic rank mod 2) | Nekovar; T. and V. Dokchitser |
| BSD over $\mathbb{F}_q(C)$ | THEOREM under finite Sha (template, not a proof over $\mathbb{Q}$) | Tate; Artin-Tate; Milne 1975 |
| Population statements | Positive proportion of $E/\mathbb{Q}$ satisfy BSD; average rank bounded | Bhargava-Shankar; Bhargava-Skinner-Zhang |

## The architectures and their walls

| # | Architecture | Reaches | The wall | Detector that catches it |
|---|---|---|---|---|
| 1 | **Modularity** (Wiles; BCDT) | analytic continuation, functional equation, root number $w$ | gives rank mod 2 only, not the rank | Detector 1 (parity-only) |
| 2 | **Heegner / Gross-Zagier + Kolyvagin** | rank 0 and 1, fully (rank equality + finite Sha) | a single Heegner point is a rank-1 object; cannot exhibit two independent generators | the rank-boundary wall itself |
| 3 | **Iwasawa theory / main conjectures** (Mazur; Kato; Skinner-Urban) | $p$-part of strong BSD in many rank $\leq 1$ cases; $p$-adic BSD | archimedean leading term and unconditional Sha-finiteness in rank $\geq 2$ | Detector 2 (Sha) |
| 4 | **Euler systems** (Kolyvagin, Kato) | bounds on Selmer / Sha | known systems are tied to rank $\leq 1$ or the analytic side; a higher-rank system is missing | the rank-boundary wall |
| 5 | **Statistics / averages** (Bhargava et al.) | positive proportion satisfy BSD; bounded average rank | a statement about the family, not about a given $E$ | (no single detector; it is a different kind of claim) |
| 6 | **Function-field analog** (Tate; Artin-Tate) | full BSD over $\mathbb{F}_q(C)$ under finite Sha | imports a geometric Frobenius the number-field case lacks | Detector 3 (function-field mirage) |

## Source scorecard (per reading note)

Honest per-source scoring against the three detectors and the rank control pair. Regime column states the EXACT range each source certifies. "Reaches" is what the source *proves*, never a conjecture upgraded to a theorem.

| Source | Regime certified | Reaches | Detector 1 (parity-only) | Detector 2 (Sha assumed) | Detector 3 (function-field) | Rank control pair | Note |
|---|---|---|---|---|---|---|---|
| Silverman, AEC (GTM 106, 2nd ed., 2009), Ch VIII + X | FOUNDATIONAL, regime-neutral: all ranks, all number fields; ARCHIMEDEAN (canonical height, real regulator); defines rank, $\operatorname{Reg}$, $\operatorname{Sel}^{(m)}$, $\operatorname{Sha}$ | unconditional theorems: $E(K)$ finitely generated (Mordell-Weil, VIII.6.7 weak form), $\hat h$ positive-definite quadratic form giving $\operatorname{Reg}$ (VIII.9), $\operatorname{Sel}^{(m)}$ finite and computable (X.4.2), descent exact sequence $0\to E(K)/mE(K)\to\operatorname{Sel}^{(m)}\to\operatorname{Sha}[m]\to 0$ (X.4); does NOT compute the rank and does NOT prove $\#\operatorname{Sha}<\infty$ | N/A (passes): no $L$-function, no root number, no parity in GTM 106; supplies the true rank invariant the parity detector measures against | SOURCE / baseline: finiteness of $\operatorname{Sha}$ is stated AS AN OPEN CONJECTURE (X.4, Shafarevich-Tate); the book itself declines to assume it, so this is the honest baseline the detector is calibrated against | PASSES (n/a): number-field-native; $H^1(G_{\bar K/K},-)$, local conditions at places of $K$, Northcott height finiteness; no geometric Frobenius, no Artin-Tate Brauer machinery | DEFINES both ends of the pair: the rank-$1$ vs rank-$\geq 2$ distinction is INVISIBLE at the definition level (descent sequence identical for $37a1$ and $389a1$); the wall appears only once a Selmer bound or $L$-value is imported, neither in this book | [`Silverman-AEC-GTM106.md`](../03_research/reading_notes/Silverman-AEC-GTM106.md) |
| Cassels 1966 (J. London Math. Soc. s1-41, 193-291) | FOUNDATIONAL, rank-neutral; number field $K$; ARITHMETIC / global Galois cohomology; no analytic-rank hypothesis (pre-modularity, $L$-side conjectural) | the descent exact sequence $0\to E(K)/nE(K)\to\operatorname{Sel}^{(n)}\to\operatorname{Sha}[n]\to 0$ with $\operatorname{Sel}^{(n)}$ finite/computable; torsor interpretation of $H^1(K,E)$ and definition of $\operatorname{Sha}$ (the "Ш" notation, local-global obstruction); the Cassels-Tate pairing (bilinear, kernel $=$ divisible part, alternating for $E$), hence **$\operatorname{Sha}$ finite $\Rightarrow\#\operatorname{Sha}$ a perfect square** | PASSES (n/a): no parity/root-number claim; pre-modularity, supplies the $\operatorname{Sha}$ in which parity is later expressed but makes no rank-mod-2 statement | ORIGIN OF THE DETECTOR'S OBJECT, scrupulously honest: square-order is EXPLICITLY conditional on $\operatorname{Sha}$ finite; finiteness stated as a CONJECTURE; the pairing constrains shape, never proves finiteness | PASSES: number-field Galois cohomology, no geometric Frobenius; the clean baseline against which the Tate/Artin-Tate function-field mirage (BSD over $\mathbb{F}_q(C)$ under finite Sha) is recognized | UNDERNEATH the pair: defines $\operatorname{Sel}$ and $\operatorname{Sha}$ identically in both regimes; the rank-$\leq 1$ proofs (GZK) supply finiteness for THESE objects, the rank-$\geq 2$ open problem is to populate THESE objects; the ruler, not the measurement | [`Cassels-1966-Diophantine-Elliptic-Curves.md`](../03_research/reading_notes/Cassels-1966-Diophantine-Elliptic-Curves.md) |
| Coates-Wiles 1977 (Invent. math. 39, 223-251) | analytic rank 0 only, and only the $L(1)\neq 0$ side; CM by $\mathcal{O}_K$ with $h_K=1$; $E/K$ or $E/\mathbb{Q}$; $p$-adic machinery, ARCHIMEDEAN conclusion | one half of weak BSD: $g_F\geq 1\Rightarrow L(E/F,1)=0$, i.e. $L(E/F,1)\neq 0\Rightarrow\operatorname{rank}E(F)=0$ (Thm 1); proven via elliptic-unit Euler system bounding $(U_n'/\bar C_n)^{(1)}$ by the $L$-value mod $\mathfrak{p}$ (Thm 29, Cor 32) and a point of infinite order forcing $H_nF_\infty/F_\infty$ ramified (Lem 35) | PASSES: gives exact rank 0 on the $L(1)\neq 0$ locus, not rank mod 2; root number plays no role | PASSES (clean): never assumes $\#\operatorname{Sha}<\infty$; authors EXPLICITLY flag Sha-finiteness as open (p. 224, "we have not succeeded"); the units bound is unconditional | PASSES (n/a): elliptic units in ray class fields of $K$, Lubin-Tate formal group, arithmetic Frobenius $\pi=\psi(\mathfrak{p})$; no geometric Frobenius, no function-field shortcut | proven side, EASIEST end: rank 0 only and CM only (below the GZK rank-$\leq 1$ ceiling in generality but its ancestor); a one-point / non-existence machine, structurally cannot touch rank $\geq 2$ | [`Coates-Wiles-1977-BSD.md`](../03_research/reading_notes/Coates-Wiles-1977-BSD.md) |
| Gross-Zagier 1986 (Invent. math. 84, 225-320) | analytic rank 1 (and boundary rank 0); ARCHIMEDEAN; Heegner hypothesis ($\varepsilon(p)=1$ for all $p\mid N$, $D$ coprime to $N$) | the height-derivative identity $L'(f,\chi,1)=\tfrac{8\pi^2}{u^2\sqrt{\lvert D\rvert}}\hat{h}(c_{\chi,f})$ (Thm 6.3); over $K$, $L'(E/K,1)=c\,\hat{h}(P_K)$, $c>0$ (Thm 2.1); $L'(E,1)\neq 0\Rightarrow E(\mathbb{Q})$ has a point of infinite order (Thm 7.3); $L'(f,\chi,1)\geq 0$ (Cor 1.1); effective class-number bound (Thm 8.1) | PASSES: gives an exact real identity $L'=c\,\hat h$ and an actual non-torsion point, strictly stronger than parity | PASSES: proven statements assume no finiteness; the $\operatorname{Sha}$ dependence is isolated and LABELLED conjectural in Conj 2.2/2.3 (does not bound $\operatorname{Sha}$ itself) | PASSES (n/a): archimedean Green's function on $X_0(N)(\mathbb{C})$ + arithmetic-surface intersection theory over $\mathbb{Z}$; no geometric Frobenius | proven side of the pair; STRUCTURALLY CAPPED: outputs ONE point, certifies rank exactly 0 or 1, never $\geq 2$ (witness: Prop 7.4, an analytic-rank-3 curve where $P_K$ is torsion) | [`Gross-Zagier-1986-Heegner-Points-Derivatives.md`](../03_research/reading_notes/Gross-Zagier-1986-Heegner-Points-Derivatives.md) |
| Kolyvagin 1990 ("Euler systems," Grothendieck Festschrift II, Progr. Math. 87, 435-483) | analytic rank 0 and 1 ($y_K$ torsion / non-torsion); number-field Galois cohomology, ARCHIMEDEAN-seeded; Heegner hypothesis, big-image $\operatorname{Gal}(\mathbb{Q}(E_p)/\mathbb{Q})\cong\operatorname{GL}_2(\mathbb{Z}/p)$ | from one non-torsion Heegner point: $\operatorname{Sel}_p(E/K)$ cyclic generated by $\delta(y_K)$, hence $\operatorname{rank} E(K)=1$ and $\operatorname{Sha}(E/K)$ FINITE (PROVEN, not assumed); rank 0 + finite $\operatorname{Sha}$ when $y_K$ torsion and $L(E/K,1)\neq 0$ | PASSES: produces the actual rank value and the actual triviality of $\operatorname{Sha}_p$, not rank mod 2; strictly stronger than the root-number parity | PASSES and is the SOURCE: $\operatorname{Sha}$ finiteness is PROVEN here, but only because the Euler system has a single non-torsion seed (rank 1); the bound $\dim\operatorname{Sel}_p\leq 1+2\operatorname{ord}_p[\,\cdots]$ is finite precisely because there is one seed | PASSES (n/a): genuinely number-field-native; Chebotarev over $K(E_p)/\mathbb{Q}$, Tate local duality at finite places, imaginary-quadratic $K_\infty=\mathbb{C}$ kills $H^1(\infty)$, Eichler-Shimura on $X_0(N)$; no geometric Frobenius shortcut | proven side; STRUCTURAL CEILING is one degree of freedom ($P_1=y_K$), so certifies rank $\leq 1$ and can never bound a second Selmer dimension; the axioms (norm relation $\operatorname{Tr}_\ell y_n=a_\ell y_m$ + Frobenius congruence + sign $\epsilon_n$) specify what a rank-$\geq 2$ system must reproduce | [`Kolyvagin-1990-Euler-Systems.md`](../03_research/reading_notes/Kolyvagin-1990-Euler-Systems.md) |
| Bhargava-Skinner-Zhang 2014 (arXiv:1407.1826) | each certified curve is *individually* analytic rank 0 or 1; $p$-adic + archimedean hybrid | $> 66.48\%$ of $E/\mathbb{Q}$ (by height) satisfy BSD rank equality and have finite $\operatorname{Sha}$ (Thms 1, 2); $\geq 16.50\%$ rank 0, $\geq 20.68\%$ rank 1 (Thm 3) | PASSES: parity (Dokchitser-Dokchitser $p$-parity, root-number equidistribution) is necessary but is upgraded to a rank value only by the Selmer-*size* average plus rank-$\leq 1$ criteria | PASSES: $\operatorname{Sha}$ finiteness is an OUTPUT via Gross-Zagier-Kolyvagin after analytic rank $\leq 1$, never an input; delivers zero new finiteness in the open regime | PASSES (n/a): over $\mathbb{Q}$ throughout; Selmer counts are geometry-of-numbers, no elliptic surface or geometric Frobenius | proven slice $=$ exactly the rank-$\leq 1$ (GZK-reachable) part; open complement $< 33.52\%$ is where rank $\geq 2$ lives; Thm 27's conditional $100\%$ is density-only and still misses rank $\geq 2$ | [`Bhargava-Skinner-Zhang-2014-Majority-BSD.md`](../03_research/reading_notes/Bhargava-Skinner-Zhang-2014-Majority-BSD.md) |
| Skinner-Urban 2014 (Invent. math. 195, 1-277) | $p$-adic, $p$ ordinary; cyclotomic main conjecture *equality* under hypotheses; the $p$-part of strong BSD in analytic rank 0; a one-sided Selmer-corank statement at analytic rank 1 | reverse divisibility $Ch(f)\subseteq(\mathcal{L}_f)$ via Eisenstein congruences on $GU(2,2)$ (Thm 3 $=$ 3.6.1); with Kato, equality $Ch(f)=(\mathcal{L}_f)$ (Thm 1 $=$ 3.6.4); hence $\lvert L(E,1)/\Omega_E\rvert_p^{-1}=\#\operatorname{III}(E/\mathbb{Q})_p\cdot\prod_{\ell\mid N}c_\ell$ in rank 0 (Thm 2(a) $=$ 3.6.11), and $L(E,1)=0\Rightarrow\operatorname{corank}Sel_{p^\infty}\geq 1$ (Thm 2(b)) | PASSES: gives the exact $p$-adic valuation of the leading term, far finer than rank mod 2; structurally not a parity method | TRIGGERS but honestly: *proves* (not assumes) finiteness of $\operatorname{III}_p$ and computes $v_p(\#\operatorname{III})$, only in the rank-0 unconditional ordinary window; says nothing in rank $\geq 2$; global $\#\operatorname{III}<\infty$ stays open | PASSES: genuine number-field argument; the geometry is the $GU(2,2)$ Shimura variety over $\mathbb{Q}$, not a geometric Frobenius over $\mathbb{F}_q(C)$ | sits entirely on the proven side: rank 0 (Thm 2a) and a $p$-adic half of rank 1 (Thm 2b); the single Hida family $\mathbf{f}$ feeding the Eisenstein congruence cannot reach rank $\geq 2$ | [`Skinner-Urban-2014-Iwasawa-Main-GL2.md`](../03_research/reading_notes/Skinner-Urban-2014-Iwasawa-Main-GL2.md) |
| Mazur-Tate-Teitelbaum 1986 (Invent. math. 84) + Greenberg-Stevens 1993 (Invent. math. 111) | $p$-adic, $p$ good ordinary or multiplicative; PROVEN layer is the exceptional-zero first derivative in the split-multiplicative analytic-rank-$0$ case; $p$-adic BSD (order $=$ rank, $p$-adic leading term) CONJECTURAL | the $p$-adic $L$-function $L_p(E,s)$ via modular symbols and its interpolation factor $(1-1/\alpha)^2$ / $(1-1/\alpha)$; the exceptional zero (split mult.: $\alpha=1$, factor $0$, extra zero); the $\mathcal{L}$-invariant $\mathcal{L}_p(E)=\log_p(q_E)/\operatorname{ord}_p(q_E)$ and $L_p'(E,1)=\mathcal{L}_p(E)\,L(E,1)/\Omega_E$ (Greenberg-Stevens) | PASSES: the $\mathcal{L}$-invariant and $p$-adic leading term are exact-order data, finer than parity; the exceptional zero separates the $p$-adic order from the parity-controlled complex order by a known $+1$ | THE STRUCTURAL WALL: $L_p$ bounds nothing alone; the bridge to $\#\mathrm{Sha}[p^\infty]$ is the main conjecture (Kato + Skinner-Urban), a conditional INPUT, one prime at a time; the exceptional zero adds a derivative, never a Sha bound; rank $\geq 2$ untouched | PASSES (n/a): cyclotomic $\mathbf{Z}_p$-extension + weight deformation replace the geometric Frobenius; modular symbols and Hida families over $\mathbf{Q}$, no elliptic surface, no Brauer group | proven side, one prime over: a $p$-adic FIRST-derivative formula, the exact analog of the Gross-Zagier rank-$1$ ceiling; $\mathcal{L}_p(E)$ stays computable in rank $\geq 2$ but is one factor of a leading term whose $p$-adic regulator and $\#\mathrm{Sha}$ are out of reach | [`Mazur-Tate-Teitelbaum-1986-p-adic-BSD.md`](../03_research/reading_notes/Mazur-Tate-Teitelbaum-1986-p-adic-BSD.md) (computed: [experiment (k)](../../experiments/padic_lfunction/e_k_padic_lfunction.py)) |
| Ulmer 2002 (Ann. of Math. 155, 295-315) | FUNCTION FIELD $\mathbb{F}_q(t)$, char $p$; UNBOUNDED rank (rank $\to\infty$); full BSD PROVEN in all ranks for $y^2+xy=x^3-t^d$, $d\mid p^n+1$ | $\operatorname{rank}E(\mathbb{F}_q(t))\geq(p^n-1)/2n$, unbounded (Thm 1.5); EXACT closed-form rank $\sum_{e\mid d,\,e\nmid 6}\phi(e)/o_e(q)+(\text{2,3-terms})$ (Thm 9.2); full BSD = rank equality AND refined formula (via Tate 1966 / Milne 1975), proven through the Tate conjecture for the surface $\mathcal{E}$ dominated by the Fermat surface $F_d$ (Prop 6.4); pole order of $\zeta(\mathcal{E},s)$ at $s=1$ counted by Jacobi sums $J(a)=q^{u(a)}$ (Prop 7.6, 8.1) | PASSES (over-delivers): computes the EXACT rank in closed form and proves full rank equality, the opposite of a parity collapse | PASSES: does not assume finite $\operatorname{Sha}$; PROVES the Tate conjecture for $\mathcal{E}$ outright (Fermat domination + Deligne purity), which YIELDS BSD and hence finite $\operatorname{Sha}$ as an output | TRIGGERS MAXIMALLY, BY DESIGN: the canonical mirage exhibit; full BSD in unbounded rank delivered by geometric Frobenius on $H^2$ of a surface over $\mathbb{F}_q$, the Tate conjecture, and a Gauss/Jacobi-sum count; the rank even DEPENDS on $\#\mathbb{F}_q$ (jumps when constants enlarged), no number-field analog | LIVES ENTIRELY in the open rank-$\geq 2$ (indeed rank $\to\infty$) regime, and proves it; shows the wall over $\mathbb{Q}$ is NOT intrinsic to high rank but is the absence of the geometric-Frobenius / Tate engine; needs NO Euler system (Tate replaces descent) | [`Ulmer-2002-Large-Rank-Function-Fields.md`](../03_research/reading_notes/Ulmer-2002-Large-Rank-Function-Fields.md) |
| Milne 1975 (Ann. of Math. 102, 517-533) | FUNCTION FIELD $K=\mathbb{F}_q(C)$, $p\neq 2$; both prime-to-$p$ and $p$-part; rank-UNIFORM (all $r\geq 0$, no rank-1 ceiling); geometric, not over $\mathbb{Q}$ | (T) $\Rightarrow$ full (A-T) including the $p$-part (Thm 6.1), via Poincare duality making $\operatorname{Br}(X)$ self-dual (Thm 2.4) and the slope/Euler-char identity $\alpha(X)=\sum_{v(a_{2,i})<1}(1-v(a_{2,i}))+s$ (Thm 7.1); hence over $K$ the equivalence (a) weak BSD $\iff$ (b) some $\operatorname{III}(E/K)(l)$ finite $\iff$ (c) $\operatorname{III}(E/K)$ finite AND strong BSD $L^*(E,s)\sim\tfrac{[\operatorname{III}]\det\langle a_i',a_j\rangle}{[E(K)_{\mathrm{tors}}]^2}(s-1)^r$ (Thm 8.1) | PASSES: full strong-form identity (exact order $r=\operatorname{rank}$, leading coeff with $[\operatorname{III}]$, regulator, torsion); parity / root number plays no role | TRIGGERS, transparently: the theorem is an *equivalence* pivoting on finite $\operatorname{III}(E/K)=\operatorname{Br}(X)$ (Thm 8.1(b)); finiteness is named, load-bearing, discharged only where (T) is known (rational / supersingular-K3 / product surfaces); no unconditional finiteness over $\mathbb{Q}$ | TRIGGERS, CANONICALLY (sharpest form): the proof IS the geometric Frobenius $\Phi$ on $\bar X$ plus Poincare duality on a *surface* and crystalline / flat / Witt-vector cohomology; none of this exists over $\mathbb{Q}$; a $\mathbb{Q}$-method that transcribes verbatim to $\mathbb{F}_q(C)$ has imported the surface | NO WALL over $K$: rank $\geq 2$ is handled by the same cohomology as rank 0/1 because the proof constructs no point and so is never capped at one; the rank-1 ceiling is exposed as a number-field artifact of the missing geometric Frobenius | [`Milne-1975-Artin-Tate.md`](../03_research/reading_notes/Milne-1975-Artin-Tate.md) |
| Kudla 2004 (MSRI Publ. 49, survey of the program) | number-field arithmetic geometry; PROVEN layer parallels rank $\leq 1$ (first central derivatives: Kudla 1997 genus-2 coefficients on Shimura curves, Kudla-Rapoport-Yang 2006 arithmetic theta + inner product, Li-W. Zhang 2021 unitary Kudla-Rapoport); higher-derivative layer CONJECTURAL | arithmetic special cycles $\widehat{Z}(T,v)$ in all codimensions, generating series modular (proven over $\mathbb{C}$: GKZ, Borcherds, Bruinier-Westerholt-Raum); arithmetic Siegel-Weil in low dimension; NO second Taylor coefficient of any $L$-function over a number field | PASSES: exact height/intersection identities, strictly finer than parity | PASSES (n/a): no $\operatorname{Sha}$ input or output; pairs with Direction 02 rather than replacing it | PASSES with flagged caution: integral models over $\mathbb{Z}$, honest Arakelov theory, no geometric Frobenius; BUT its higher-derivative layer is exactly what is a theorem only over function fields (Yun-Zhang), so any transfer claim gets the two-import audit | does not yet run on the pair: outputs cycle classes, not points; the ONLY input class whose structural shape (genus-$r$ Eisenstein vs codim-$r$ cycles) matches Direction 01 Clause 2 over $\mathbb{Q}$ | [`Kudla-2004-Special-Cycles-Derivatives-Eisenstein.md`](../03_research/reading_notes/Kudla-2004-Special-Cycles-Derivatives-Eisenstein.md) |
| Yun-Zhang 2017/2019 (Ann. of Math. 186, 189) | FUNCTION FIELD $F = \mathbb{F}_q(X)$, everywhere-unramified $PGL_2$ (part I), Iwahori level (part II); ALL central derivatives $r \geq 0$ | $L^{(r)}(\pi_{F'}, 1/2) \sim \langle [\mathrm{Sht}^r_T]_\pi, [\mathrm{Sht}^r_T]_\pi

angle$: the $r$-th Taylor coefficient as self-intersection of the Heegner-Drinfeld cycle on the moduli of shtukas with $r$ legs; $r = 1$ is function-field Gross-Zagier, $r \geq 2$ has NO number-field analog; produces no rational points even there | PASSES (over-delivers): every Taylor coefficient, rank-uniform in $r$, the opposite of parity collapse | PASSES (n/a): cycle/L-function side only; rank consequences still route through Tate/Milne Brauer-finiteness | TRIGGERS BY DESIGN, the sharpest control: the mechanism needs $X^r$ (legs moving on $r$ copies of the curve) and the Frobenius twist in the shtuka definition; both imports named, neither exists over $\mathbb{Q}$ | rank-uniform by construction ($r = 1$ and $r = 2$ are the same theorem), so the comparison isolates the number-field wall to the two named imports; the most informative single contrast the program has | [`Yun-Zhang-2017-Shtukas-Taylor-Expansion.md`](../03_research/reading_notes/Yun-Zhang-2017-Shtukas-Taylor-Expansion.md) |

## The cross-cutting thesis

**The rank-boundary thesis.** Every proven technique bottoms out at analytic rank 1 because its engine is a rank-1 object (one Heegner point, one Euler system class tied to it). A general proof must construct something new in the rank $\geq 2$ regime: either a construction producing $\geq 2$ independent rational points (a higher-rank analog of the Heegner point), or an Euler system bounding Sha unconditionally in rank $\geq 2$.

## The one missing object

A construction that, on a rank $\geq 2$ curve, produces two provably independent rational points OR bounds $\operatorname{Sha}$ unconditionally. Untouched at the construction level. Multi-year, multi-person.

The engine ([`experiments/engine/`](../../experiments/engine/)) now derives this object mechanically rather than asserting it: the frontier query for weak BSD in rank $\geq 2$ returns exactly `{rank_two_object}`, and for strong BSD `{rank_two_object, higher_euler_system}`, with no path through parity. "Where the proof must live" is a computed antichain, not a remembered paragraph.

## How to read the rest of the repo

- Per-approach detail and obstructions: [`docs/solutions/`](../solutions/).
- The proof program and research-grade specs: [`docs/03_research/`](../03_research/).
- Why it matters: [`docs/implications/`](../implications/).
- The computational thread that validates the substrate: [`experiments/PLAN.md`](../../experiments/PLAN.md).
- This atlas as a machine-queryable proof graph (the frontier computed, not asserted): [`experiments/engine/`](../../experiments/engine/), specified in [`docs/03_research/engine/`](../03_research/engine/README.md).
- The Kudla Clause-2 bridge, specified (the live candidate the engine surfaced), plus its computable rank-1 shadow: [`04_kudla_clause_two_bridge.md`](../03_research/research_directions/04_kudla_clause_two_bridge.md) and experiment (m) [`experiments/theta_shadow/`](../../experiments/theta_shadow/).
- The order gap reframed as archimedean, with the p-adic second axis (cyclotomic + weight) as the number-field second leg and the p-adic height regulator as the computed witness: [`05_padic_second_leg.md`](../03_research/research_directions/05_padic_second_leg.md) and experiment (n) [`experiments/padic_regulator/`](../../experiments/padic_regulator/).
- Operating philosophy: [`docs/researcher_mindset.md`](../researcher_mindset.md).
